# Coding Pack — Invariants Add-on

Append to `core/INVARIANTS.md`.

## INV-CODE-001
Capture a baseline run (config + binary hash + input id) before tuning.

## INV-CODE-002
Prefer primary artifacts (debug images/logs) over counters.
