# LIMO Infrastructure — Environment Variables
#
# Copy this file to .env and fill in all values before running docker compose.
# The .env file is the SINGLE SOURCE OF TRUTH for all secrets.
# NEVER commit the .env file to git.

# ── Postgres ───────────────────────────────────────────────────────
POSTGRES_USER=limo
POSTGRES_PASSWORD=          # Generate: openssl rand -base64 24
POSTGRES_DB=limo

# ── Redis ──────────────────────────────────────────────────────────
REDIS_PASSWORD=             # Generate: openssl rand -base64 24

# ── Neo4j ──────────────────────────────────────────────────────────
NEO4J_PASSWORD=             # Generate: openssl rand -base64 24

# ── OpenAI (for mem0 embeddings) ───────────────────────────────────
# mem0 uses OpenAI for text embeddings (text-embedding-3-small).
# Get your API key from https://platform.openai.com/api-keys
# Cost: roughly $0.002-0.005 per memory stored.
OPENAI_API_KEY=

# ── n8n ────────────────────────────────────────────────────────────
N8N_ENCRYPTION_KEY=         # Generate: openssl rand -hex 32
WEBHOOK_URL=https://your-server-domain/   # Your server's public URL (for webhook callbacks)
